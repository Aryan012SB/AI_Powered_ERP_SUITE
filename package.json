{
  "name": "amdox-erp",
  "version": "1.0.0",
  "private": true,
  "description": "AMX-ERP-2026-04: AI-Powered Cloud ERP Suite",
  "scripts": {
    "dev": "npm run dev --prefix apps/web",
    "build": "npm run build --prefix apps/web",
    "start": "npm run start --prefix apps/web",
    "test": "npm run test --prefix apps/web"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {}
}
